# Senex Trader Requirements
# Updated: 2025-10-12 (Docker/Container Support)
# Run 'pip index versions [package]' to check for newer versions

# Core Django
django>=5.2.7  # Updated from 5.2.6 (latest LTS patch)
python-dotenv>=1.0.0
django-extensions>=3.2.3

# Production Database & Server
psycopg2-binary>=2.9.9  # PostgreSQL 18 driver (production)
whitenoise>=6.6.0       # Static file serving

# Phase 2: OAuth & Security (install when needed)
tastytrade==11.0.1
pandas-market-calendars>=5.1.1
django-encrypted-model-fields>=0.6.5
cryptography>=46.0.2    # Updated from 45.0.7 (security patches)
requests>=2.32.5

# Phase 4: Streaming (install when needed)
channels>=4.3.1
channels-redis>=4.3.0
redis>=5.0.1
daphne>=4.2.1

# Phase 9: Background Tasks (install when needed)
celery>=5.5.3
django-celery-beat>=2.8.1

# Development Tools - Ruff-First Approach
# Primary Quality Stack
ruff>=0.13.2           # Primary linter (replaces flake8, isort, pyupgrade, etc.)
black>=24.2.0          # Code formatting
mypy>=1.18.0           # Type checking
django-stubs>=5.2.5    # Django type stubs for mypy
bandit>=1.8.0          # Security vulnerability scanning
autoflake>=2.3.0       # Remove unused imports/variables

# Testing
pytest>=8.4.0
pytest-django>=4.8.0
pytest-asyncio>=1.2.0
pytest-cov>=5.0.0      # Coverage reporting for tests

# Supplementary (Ruff handles most use cases - optional, kept for manual use)
isort>=6.1.0           # Import sorting (backup - ruff primary)
pylint>=3.3.0          # Additional analysis (slower)
flake8>=7.0.0          # Legacy linter (for compatibility)

# Utilities
tqdm>=4.66.0           # Progress bars for management commands

# Note: Always check latest versions before installation:
# Security-critical packages (check first):
# pip index versions django
# pip index versions cryptography
# Financial APIs and core packages:
# pip index versions tastytrade
# pip index versions django-encrypted-model-fields
# pip index versions channels
# pip index versions celery
# pip index versions pandas-market-calendars
